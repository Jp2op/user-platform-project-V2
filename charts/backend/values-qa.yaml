# QA environment overrides
# Only values that differ from defaults

replicaCount: 1

image:
  tag: "sha-76a813506feaaa2f9c7cd1186e7b9a79a89e75bf"  # updated by CI pipeline on every push to qa branch

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3

# ESO — which Secrets Manager secret to sync and which SA to use
externalSecret:
  secretName: "uplatform/qa/mysql-secret"

eso:
  serviceAccountName: "eso-qa-sa"

dbInit:
  enabled: true
  rootSecretName: "uplatform/rds/root"