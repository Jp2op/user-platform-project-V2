# QA environment overrides
# Only values that differ from defaults

replicaCount: 1

image:
  tag: "sha-09a441dd71df79bb1d8374c7bb49abb6777f0644"  # updated by CI pipeline on every push to qa branch

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3

# ESO — which Secrets Manager secret to sync and which SA to use
externalSecret:
  secretName: "uplatform/qa/mysql-secret"

eso:
  serviceAccountName: "eso-qa-sa"

dbInit:
  enabled: true
  rootSecretName: "uplatform/rds/root"