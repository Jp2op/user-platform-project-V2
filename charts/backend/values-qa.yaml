# QA environment overrides
# Only values that differ from defaults

replicaCount: 1

image:
  tag: "sha-89f7a3a96abae6ed89a36c44d2ea1df8adb6905e"  # updated by CI pipeline on every push to qa branch

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3

# ESO — which Secrets Manager secret to sync and which SA to use
externalSecret:
  secretName: "uplatform/qa/mysql-secret"

eso:
  serviceAccountName: "eso-qa-sa"

dbInit:
  enabled: true
  rootSecretName: "uplatform/rds/root"